package aaron.TimeTracker;

import android.widget.RemoteViews;

public class TimerNotificationView extends RemoteViews {
    public TimerNotificationView(String packageName, int layoutId) {
        super(packageName, layoutId);
    }
}
